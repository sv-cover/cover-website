Hi!

{{ member_name }} changed the following event on behalf of the {{ committee_name }}:

Name:     {{ event.kop }}
From:     {{ event.van }}
Until:    {{ event.tot }}
Location: {{ event.locatie }}
Category: {{ event.category|split(',')|map(c => c|title)|join(', ', __(' and ')) }}

{{ event.beschrijving ~}}

You can approve (or decline) this event on {{ url('events.moderate', {_fragment: 'event_' ~ proposal_id}) }} .

Kind Regards,


The WebSite
