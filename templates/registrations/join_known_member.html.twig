{% extends '@layout/layout.html.twig' %}

{% block content %}
<article class="content">
    <h1 class="title">{{ __('Membership application form') }}</h1>
    <p>{{ __('Your email address is already known to us, which means you are already in our member administration! In this case it is easier to quickly contact the secretary of Cover via secretary@svcover.nl.') }}</p>
    <p>
        <a href="{{ login_path(path('homepage')) }}" class="button is-primary is-fullwidth">{{ __('Log in') }}</a>
        <div class="divider is-fullwidth">{{ __('or') }}</div>
        <a href="{{ path('password.request') }}" class="button  is-fullwidth">{{ __('Reset your password') }}</a>
    </p>
</article>
{% endblock %}
